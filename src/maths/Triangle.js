import * as maths from "./index";

export class Triangle{
  constructor(a, b, c){
    this.a = a;
    this.b = b;
    this.c = c;
  }

  getCircumCenter(center = new maths.Vector2()){
  }

  getCircumCircle(circle = new maths.Circle){
    
  }
}
